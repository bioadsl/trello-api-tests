{
  "name": "trello-api-tests",
  "version": "1.0.0",
  "description": "Testes automatizados BDD para API do Trello com Cypress",
  "scripts": {
    "test": "cypress run"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^17.2.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.5",
    "cypress": "^12.17.4",
    "esbuild": "^0.25.5"
  }
}
